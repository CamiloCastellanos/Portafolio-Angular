import { TranslateTextPipe } from './translate-text.pipe';

describe('TranslateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
